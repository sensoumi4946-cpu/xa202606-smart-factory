[project]
name = "smart-factory-connectivity"
version = "0.1.0"
description = "Protocol adapters for XA-202606 Smart Factory platform"
requires-python = ">=3.11"
dependencies = [
    "paho-mqtt>=2.0",
    "httpx>=0.28",
    "pymodbus>=3.6,<3.13",
    "asyncua>=1.1,<2",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "smart-factory-contracts",
    "smart-factory-semantic-layer",
    "pydantic>=2.0",
    "pyserial>=3.5,<4",
]

[project.scripts]
sf-opcua-serial-gateway = "connectivity.gateways.opcua_serial:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/connectivity"]
